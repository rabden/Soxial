/** Shared spring for the Human surfaces' layoutId indicators
 *  (Profile.tsx:790 tab-pill recipe). */
export const springTransition = {
  type: 'spring' as const,
  stiffness: 450,
  damping: 38,
}
